#include <iostream>
#include <algorithm>
#include <deque>
#include <vector>

using namespace std;

int main(){
  int num, data;
  cin >> num;
  string s;
  vector<int> v;
  for (int i = 0; i < num; i++){
    int num1;
    cin >> num1;
    for (int j = 0; j < num1; j++){
      cin >> s >> data;
      if(s == "I")
        {
          if(v[i] > v[i+1]) v.push_back
        }
    }
  }
}